import React from "react";
import {Space} from "antd";
import useCapitalize from "./useCapitalize";
import useRandomColor from "./useRandomColor";

export default function useGetLabels(allLabels = []) {
  return allLabels.map((label) => {

    return <>

    </>
  })
}